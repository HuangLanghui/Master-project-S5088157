# Pinned to the versions the reported results were produced with.
# Install the loose ranges instead with:  pip install -e ".[eval,experiments]"
#
# GPU note: the torch pin below is the CPU wheel. For the generative
# background (Stage 5) and generative relighting (Stage 6c) install the CUDA
# build first, then this file:
#   pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu121

pillow==11.3.0
numpy==2.2.6
opencv-python==4.12.0.88
scikit-image==0.26.0

# Experiment harness
pandas==2.3.3
matplotlib==3.10.8
scipy==1.16.2

# Tests
pytest==9.0.2
